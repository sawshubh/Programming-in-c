#include <stdio.h>

int main(int argc, char const *argv[])
{
    printf("Hello World !");
    return 0;
}

// c language is a case sensative language and a middle level language which binds gap between machine and programmer
// tokens in c languages
//smallest elements of a program, which are meaningful for the compiler
// tokens: keywords, constant, strings, operators, special symbol, literal
// keywords are the reserved words in c language, it has 32 general purpose language
